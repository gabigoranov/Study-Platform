namespace StudyPlatform.Data.Types
{
    /// <summary>
    /// A predefined set of material group types. Used to categorize material subgroups in each subject.
    /// </summary>
    public enum MaterialGroupType
    {
        /// <summary>
        /// A type for flashcards.
        /// </summary>
        Flashcards = 1,
    }
}
